---
plugin_routing:
  action:
    correlation_search_info:
      redirect: splunk.es.splunk
    correlation_searches:
      redirect: splunk.es.splunk
    data_inputs_monitor:
      redirect: splunk.es.splunk
    data_inputs_network:
      redirect: splunk.es.splunk
    adaptive_response_notable_events:
      redirect: splunk.es.splunk
  modules:
    correlation_search:
      tombstone:
        removal_date: "2025-12-10"
        warning_text: Use splunk.es.splunk_correlation_searches instead.
    correlation_search_info:
      redirect: splunk.es.splunk_correlation_search_info
    correlation_searches:
      redirect: splunk.es.splunk_correlation_searches
    data_input_monitor:
      tombstone:
        removal_date: "2025-12-10"
        warning_text: Use splunk.es.splunk_data_inputs_monitor instead.
    data_inputs_monitor:
      redirect: splunk.es.splunk_data_inputs_monitor
    data_input_network:
      tombstone:
        removal_date: "2025-12-10"
        warning_text: Use splunk.es.splunk_data_inputs_network instead.
    data_inputs_network:
      redirect: splunk.es.splunk_data_inputs_network
    adaptive_response_notable_event:
      tombstone:
        removal_date: "2025-12-10"
        warning_text: Use splunk.es.splunk_adaptive_response_notable_events instead.
    adaptive_response_notable_events:
      redirect: splunk.es.splunk_adaptive_response_notable_events
requires_ansible: ">=2.15.0"
